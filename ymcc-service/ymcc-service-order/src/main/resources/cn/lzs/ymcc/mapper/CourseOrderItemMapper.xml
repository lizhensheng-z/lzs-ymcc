<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lzs.ymcc.mapper.CourseOrderItemMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.lzs.ymcc.domain.CourseOrderItem">
        <id column="id" property="id" />
        <result column="order_id" property="orderId" />
        <result column="amount" property="amount" />
        <result column="count" property="count" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="course_id" property="courseId" />
        <result column="course_name" property="courseName" />
        <result column="course_pic" property="coursePic" />
        <result column="version" property="version" />
        <result column="order_no" property="orderNo" />
    </resultMap>
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO t_course_order_item (
        order_id,
        amount,
        count,
        create_time,
        update_time,
        course_id,
        course_name,
        course_pic,
        version,
        order_no
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.orderId},
            #{item.amount},
            #{item.count},
            #{item.createTime},
            #{item.updateTime},
            #{item.courseId},
            #{item.courseName},
            #{item.coursePic},
            #{item.version},
            #{item.orderNo}
            )
        </foreach>
    </insert>
    <select id="getCourseIdByOrderNo" resultType="java.lang.Long" parameterType="java.lang.String">
        select  course_id
        from t_course_order_item
        where order_no = #{orderNo}
    </select>


</mapper>
